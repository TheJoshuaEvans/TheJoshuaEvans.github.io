# Joshua Evans

My homepage! Welcome!

Hosted via GitHub pages. This page is just pure HTML + CSS + JS. Host the static site by running the following python command from the project root
```sh
python -m http.server 8080
```

> Note: The [CNAME](/CNAME) file is required for requests to `thejoshuaevans.com` to resolve correctly
